/** Public website — full MorseTime product (Reddit app is the daily game + extras). */
export const MORSETIME_WEB_URL = 'https://morsetime.com';

/** Outbound CTA label (Devvit shows a confirm dialog for external URLs). */
export const MORSETIME_WEB_TRAINING_LABEL = 'Full training on the web →';
